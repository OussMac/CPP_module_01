#ifndef REPLACE_HPP
#define REPLACE_HPP

#include <string>
#include <fstream>
#include <iostream>

bool performReplace(const std::string &filename,  const std::string &s1, const std::string &s2);

#endif
